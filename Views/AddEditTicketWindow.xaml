<Window x:Class="SupportServiceApp.Views.AddEditTicketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Заявка" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Заголовок"/>
        <TextBox x:Name="TitleBox" Grid.Row="0" Margin="0,20,0,0"/>

        <Label Content="Описание" Grid.Row="1"/>
        <TextBox x:Name="DescriptionBox" Grid.Row="1" Margin="0,20,0,0"/>

        <Label Content="Клиент" Grid.Row="2"/>
        <ComboBox x:Name="ClientCombo" Grid.Row="2" Margin="0,20,0,0" DisplayMemberPath="Name"/>

        <Label Content="Оборудование" Grid.Row="3"/>
        <ComboBox x:Name="EquipmentCombo" Grid.Row="3" Margin="0,20,0,0" DisplayMemberPath="Name"/>

        <Label Content="Категория" Grid.Row="4"/>
        <ComboBox x:Name="CategoryCombo" Grid.Row="4" Margin="0,20,0,0" DisplayMemberPath="Name"/>

        <StackPanel Orientation="Horizontal" Grid.Row="5" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Click="Save_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
